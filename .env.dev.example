# Development Environment Configuration
# Copy this file to .env.dev and update with your settings

# Server Configuration
NODE_ENV=development
PORT=3001

# Redis Configuration (Docker network)
# For Docker: use service name 'redis'
# For local development: use 127.0.0.1:6379
REDIS_URL=redis://redis:6379

# LLM API URL (vLLM chat completions endpoint)
LLM_API_URL=http://your-llm-server:8000/v1/chat/completions

# LLM Model path
# Example: /data/models/gpt-oss-120b
LLM_MODEL=/path/to/your/model

# SSL Configuration
# Set to 0 for development with self-signed certificates
NODE_TLS_REJECT_UNAUTHORIZED=0

# CORS Configuration
# Allow multiple origins separated by commas
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
